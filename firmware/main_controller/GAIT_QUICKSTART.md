# Быстрый старт - Система походок

## Команды для тестирования

### 1. Базовая инициализация
```
// Запустите робота, он автоматически встанет в стойку
gait stand
```

### 2. Простое тестирование походок

**Стойка (Stand):**
```
gait stand
stab on
// Наклоните робота - он должен компенсировать наклон
```

**Рысь (Trot) - Рекомендуется для начала:**
```
gait trot
motors on
stab on
// Дайте команду движения вперед с пульта
```

**Медленный шаг (Walk) - Самая стабильная:**
```
gait walk
motors on
// Медленное и стабильное движение
```

**Ползание (Crawl) - Для сложных условий:**
```
gait crawl
motors on
// Очень медленное движение, максимальная стабильность
```

**Прыжок (Bound) - Для опытных пользователей:**
```
gait bound
motors on
// ⚠️ Осторожно! Быстрая и динамичная походка
```

### 3. Настройка высоты
```
h 80    // Низкая стойка
h 100   // Средняя стойка (рекомендуется)
h 120   // Высокая стойка
```

### 4. Полная последовательность тестирования

```bash
# Шаг 1: Инициализация
gait stand
stab on
ik on

# Шаг 2: Проверка стабилизации
# (наклоните робота, он должен выровняться)

# Шаг 3: Тест походки Trot
gait trot
h 100
motors on
# Дайте команду вперед с пульта

# Шаг 4: Остановка
motors off
gait stand

# Шаг 5: Тест других походок
gait walk
motors on
# ... движение ...
motors off

gait crawl
motors on
# ... медленное движение ...
motors off
```

## Рекомендуемый порядок освоения

### Уровень 1: Начинающий
1. ✅ `gait stand` - освойте стойку и стабилизацию
2. ✅ `gait walk` - медленная стабильная походка
3. ✅ Тестирование на ровной поверхности

### Уровень 2: Продвинутый
1. ✅ `gait trot` - быстрая походка
2. ✅ Тестирование на разных поверхностях
3. ✅ Регулировка высоты в движении

### Уровень 3: Эксперт
1. ✅ `gait crawl` - экстремальные условия
2. ✅ `gait bound` - высокоскоростное движение
3. ✅ Комбинирование походок

## Отладка

### Проблема: Робот падает
**Решение:**
```
gait stand
stab on
h 90    // Уменьшите высоту
gait walk  // Используйте более стабильную походку
```

### Проблема: Ноги двигаются неправильно
**Решение:**
```
ik off
// Проверьте калибровку серво
ik on
gait stand
```

### Проблема: Робот движется слишком медленно
**Решение:**
```
gait trot  // Переключитесь на более быструю походку
// или
gait bound  // Максимальная скорость
```

### Проблема: Недостаточная стабильность
**Решение:**
```
gait walk   // Самая стабильная походка
stab on     // Включите стабилизацию
h 80        // Уменьшите высоту для большей стабильности
```

## Таблица быстрого выбора походки

| Условие | Походка | Команда |
|---------|---------|---------|
| 🛑 Остановка | Stand | `gait stand` |
| 🏃 Обычное движение | Trot | `gait trot` |
| 🚶 Неровная поверхность | Walk | `gait walk` |
| 🐢 Сложные условия | Crawl | `gait crawl` |
| 🦘 Максимальная скорость | Bound | `gait bound` |

## Сочетания клавиш (Serial Monitor)

Создайте макросы для быстрого переключения:

```
F1 -> gait stand; stab on
F2 -> gait walk; motors on; h 100
F3 -> gait trot; motors on; h 100
F4 -> motors off; gait stand
```

## Контрольный список перед запуском

- [ ] Робот откалиброван (`ik on`)
- [ ] Стабилизация включена (`stab on`)
- [ ] Установлена безопасная высота (`h 100`)
- [ ] Выбрана подходящая походка для условий
- [ ] Свободное пространство вокруг робота
- [ ] Готовность остановить моторы (`motors off`)

## Экспериментальные настройки

После освоения базовых походок, попробуйте:

```cpp
// В коде можно настроить параметры:
trotGait.setStepHeight(40);    // Увеличить высоту шага
trotGait.setFrequency(1.5);    // Увеличить скорость
walkGait.setStepLength(50);    // Увеличить длину шага
```

**Happy Walking! 🤖🦿**
