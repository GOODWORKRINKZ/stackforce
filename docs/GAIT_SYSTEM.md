# Система походок (Gait System)

## Описание

Система походок обеспечивает автоматическое управление движением всех 4 ног робота. Каждая походка - это паттерн координированного движения ног, оптимизированный для определенных условий.

## Архитектура

### Базовый класс `Gait`
Абстрактный класс с виртуальным методом `update()`, который вычисляет целевые позиции для всех ног на основе:
- Команды движения (forward/backward)
- Команды поворота (turn)
- Высоты стойки (height)
- Данных IMU (pitch, roll)
- Текущей скорости робота

### Параметры походки (`GaitParams`)
```cpp
struct GaitParams {
    float forward;   // -1.0 (назад) ... 1.0 (вперед)
    float turn;      // -1.0 (влево) ... 1.0 (вправо)
    float height;    // Высота стойки (мм)
    float pitch;     // Тангаж от IMU (градусы)
    float roll;      // Крен от IMU (градусы)
    float speed;     // Текущая скорость (для адаптации)
};
```

### Целевые позиции (`GaitTargets`)
```cpp
struct GaitTargets {
    LegTarget frontLeft;   // {x, y} для передней левой ноги
    LegTarget frontRight;  // {x, y} для передней правой ноги
    LegTarget backLeft;    // {x, y} для задней левой ноги
    LegTarget backRight;   // {x, y} для задней правой ноги
};
```

## Реализованные походки

### 1. Stand (Стойка) 🧍
**Описание:** Робот стоит на месте с активной стабилизацией

**Характеристики:**
- Все ноги на земле
- Активная компенсация крена и тангажа
- Используется как базовая позиция

**Когда использовать:**
- Остановка на месте
- Калибровка сенсоров
- Переключение между походками

**Команда:** `gait stand`

---

### 2. Trot (Рысь) 🏃
**Описание:** Диагональные пары ног движутся синхронно

**Характеристики:**
- Скорость: Высокая
- Стабильность: Средняя
- Частота шагов: 1.0 Гц (настраиваемая)
- Высота шага: 30 мм
- Длина шага: 50 мм
- Duty Cycle: 50% (половина времени - нога в воздухе)

**Паттерн движения:**
- Фаза 1: FL + BR поднимаются вместе
- Фаза 2: FR + BL поднимаются вместе

**Когда использовать:**
- Быстрое перемещение по ровной поверхности
- Длительные дистанции
- Когда нужна высокая скорость

**Команда:** `gait trot`

**Настройка:**
```cpp
trotGait.setStepHeight(40);    // Высота шага
trotGait.setStepLength(60);    // Длина шага
trotGait.setFrequency(1.2);    // Частота (Гц)
```

---

### 3. Walk (Шаг) 🚶
**Описание:** Статически стабильная походка, всегда 3 ноги на земле

**Характеристики:**
- Скорость: Низкая
- Стабильность: Очень высокая
- Частота шагов: 0.5 Гц
- Высота шага: 25 мм
- Длина шага: 40 мм

**Паттерн движения (последовательность):**
1. FR (передняя правая)
2. BL (задняя левая)
3. FL (передняя левая)
4. BR (задняя правая)

**Когда использовать:**
- Неровная поверхность
- Требуется максимальная стабильность
- Медленное точное перемещение
- Препятствия и сложный рельеф

**Команда:** `gait walk`

---

### 4. Bound (Прыжок) 🦘
**Описание:** Обе передние ноги, затем обе задние (как у зайца)

**Характеристики:**
- Скорость: Очень высокая
- Стабильность: Низкая
- Частота прыжков: 0.8 Гц
- Высота прыжка: 40 мм
- Длина прыжка: 60 мм

**Паттерн движения:**
- Фаза 1 (0.0-0.5): Передние ноги прыгают, задние толкают
- Фаза 2 (0.5-1.0): Задние ноги прыгают, передние приземляются

**Когда использовать:**
- Максимальная скорость на ровной поверхности
- Преодоление небольших препятствий
- Динамичное движение

**Команда:** `gait bound`

**⚠️ Внимание:** Требует хорошей калибровки и может быть нестабильной!

---

### 5. Crawl (Ползание) 🐢
**Описание:** Очень медленная и стабильная походка

**Характеристики:**
- Скорость: Очень низкая
- Стабильность: Максимальная
- Частота шагов: 0.3 Гц
- Высота шага: 20 мм
- Длина шага: 30 мм

**Паттерн движения (очень медленная последовательность):**
1. FR → 2. FL → 3. BR → 4. BL
- Всегда 3 ноги на земле
- Усиленная стабилизация

**Когда использовать:**
- Очень сложный рельеф
- Узкие проходы
- Требуется максимальная точность
- Скользкие поверхности

**Команда:** `gait crawl`

---

## Сравнительная таблица походок

| Походка | Скорость | Стабильность | Энергоэффективность | Применение |
|---------|----------|--------------|---------------------|------------|
| Stand   | 0        | ★★★★★        | ★★★★★              | Остановка |
| Trot    | ★★★★     | ★★★          | ★★★★               | Обычное движение |
| Walk    | ★★       | ★★★★★        | ★★★                | Сложный рельеф |
| Bound   | ★★★★★    | ★★           | ★★                 | Высокая скорость |
| Crawl   | ★        | ★★★★★        | ★★                 | Экстремальные условия |

## Использование в коде

### Базовое использование
```cpp
Robot robot;

void setup() {
    robot.init();
    robot.setGait("trot");  // Установить походку "рысь"
}

void loop() {
    robot.update();  // Походка автоматически обновляется
}
```

### Переключение походок
```cpp
// Стойка для остановки
robot.setGait("stand");

// Медленное движение по сложной поверхности
robot.setGait("walk");

// Быстрое движение по ровной поверхности
robot.setGait("trot");

// Максимальная скорость
robot.setGait("bound");

// Экстремально медленное движение
robot.setGait("crawl");
```

### Serial команды
В Serial Monitor можно переключать походки в реальном времени:

```
gait stand   - стойка
gait trot    - рысь
gait walk    - шаг
gait bound   - прыжок
gait crawl   - ползание
```

### Настройка параметров походки
```cpp
// Доступ к конкретной походке через Robot
// (требует добавления getter методов)

// Пример: настройка Trot походки
trotGait.setStepHeight(35);     // Увеличить высоту шага
trotGait.setStepLength(55);     // Увеличить длину шага
trotGait.setFrequency(1.2);     // Увеличить частоту шагов

// Пример: настройка Walk походки
walkGait.setStepHeight(30);
walkGait.setFrequency(0.6);
```

## Принцип работы

### Траектории движения ног

#### Swing Trajectory (Нога в воздухе)
```cpp
LegTarget swingTrajectory(float t, float length, float height, ...)
```
- **X**: Линейное движение от -length/2 до +length/2
- **Y**: Параболическая траектория (подъем вверх и опускание вниз)
- **t**: Нормализованное время фазы (0.0 ... 1.0)

```
Y ^
  |     /\
  |    /  \
  |   /    \
  |__/______\___> X
```

#### Stance Trajectory (Нога на земле)
```cpp
LegTarget stanceTrajectory(float t, float length, ...)
```
- **X**: Движение назад (противоположно направлению движения тела)
- **Y**: Постоянная высота (контакт с землей)

```
Y ^
  |____________
  |            > X
```

### Фазирование

Каждая походка имеет **фазу** (phase) от 0.0 до 1.0:
- Фаза автоматически обновляется на основе времени и частоты
- Разные ноги могут иметь разные фазовые смещения
- Переход между фазами обеспечивает плавное движение

### Стабилизация

Все походки (кроме Stand) включают компенсацию IMU:
```cpp
targets.frontLeft.y -= pitchComp + rollComp;
targets.frontRight.y -= pitchComp - rollComp;
targets.backLeft.y += pitchComp + rollComp;
targets.backRight.y += pitchComp - rollComp;
```

Это обеспечивает:
- Компенсацию наклона поверхности
- Стабилизацию тела робота
- Адаптацию к неровностям

## Расширение системы

### Создание собственной походки

```cpp
class CustomGait : public Gait {
private:
    float myParameter;
    
public:
    CustomGait() : Gait("Custom"), myParameter(1.0) {}
    
    GaitTargets update(const GaitParams& params) override {
        // Ваша логика вычисления позиций ног
        GaitTargets targets;
        
        // ... вычисления ...
        
        return targets;
    }
};
```

### Добавление в Robot
```cpp
// В Robot.h
CustomGait customGait;

// В Robot.cpp (метод setGait)
else if (name == "custom") {
    currentGait = &customGait;
}
```

## Отладка походок

### Визуализация фаз
```cpp
Serial.printf("Phase: %.2f | FL: (%.1f, %.1f) FR: (%.1f, %.1f)\n",
    phaseOffset,
    targets.frontLeft.x, targets.frontLeft.y,
    targets.frontRight.x, targets.frontRight.y);
```

### Проверка траекторий
- Используйте походку `stand` для базовой проверки
- Медленно увеличивайте `frequency` при тестировании
- Начинайте с малых значений `stepLength` и `stepHeight`

### Типичные проблемы

**Робот падает при движении:**
- Уменьшите `stepHeight`
- Увеличьте `dutyCycle` (больше времени на земле)
- Используйте более стабильную походку (walk/crawl)

**Недостаточная скорость:**
- Увеличьте `frequency`
- Увеличьте `stepLength`
- Используйте более быструю походку (trot/bound)

**Ноги задевают землю:**
- Увеличьте `stepHeight`
- Проверьте калибровку высоты
- Убедитесь что `params.height` достаточно велика

## Будущие улучшения

Возможные расширения:

1. **Adaptive Gait** - автоматический выбор походки на основе скорости и рельефа
2. **Terrain Following** - адаптация к наклону поверхности
3. **Dynamic Parameters** - автоматическая настройка параметров походки
4. **Gait Transitions** - плавные переходы между походками
5. **Energy Optimization** - минимизация энергопотребления
6. **Obstacle Avoidance** - обход препятствий во время движения
