# Калибровка сервоприводов и настройка робота

## 🎯 Проблема: "Ноги выгибаются как попало"

При первом включении робота сервоприводы устанавливаются в позицию 90° (по коду), но **физически** могут быть не в нужном положении из-за:
- Неточности установки серворычагов
- Механических допусков
- Различий в сервоприводах

**Решение:** Настроить оффсеты (смещения) для каждого сервопривода в файле `quadrupedal_data.h`

---

## 📐 Теория калибровки

### Как работают оффсеты:

```cpp
// В коде (main.cpp):
servos.setAngle(SERVO_FL_FRONT, angle + SERVO_FL_FRONT_OFFSET);

// Если без оффсета angle=90, а нога кривая:
servos.setAngle(SERVO_FL_FRONT, 90 + 0);  // Нога кривая

// С оффсетом исправляем:
servos.setAngle(SERVO_FL_FRONT, 90 + 15);  // Нога прямая!
```

**Оффсет** - это градусы коррекции, которые добавляются к расчетному углу.

---

## 🔧 Пошаговая калибровка

### Шаг 1: Подготовка

**Что понадобится:**
- Собранный робот с подключенными сервоприводами
- USB кабель к main контроллеру
- Линейка или штангенциркуль (опционально)
- Ровная поверхность

**Безопасность:**
1. ⚠️ **Отключите моторы** (отсоедините их от питания или контроллера)
2. Держите робота так, чтобы ноги могли свободно двигаться
3. Питание сервоприводов должно быть подключено (6V)

---

### Шаг 2: Установка нулевых оффсетов

Откройте `firmware/main_controller/include/quadrupedal_data.h`:

```cpp
// === ОФФСЕТЫ СЕРВОПРИВОДОВ (калибровка) ===
#define SERVO_FL_FRONT_OFFSET 0   // Передняя левая нога, передний серво
#define SERVO_FL_REAR_OFFSET  0   // Передняя левая нога, задний серво

#define SERVO_FR_FRONT_OFFSET 0   // Передняя правая нога, передний серво
#define SERVO_FR_REAR_OFFSET  0   // Передняя правая нога, задний серво

#define SERVO_BL_FRONT_OFFSET 0   // Задняя левая нога, передний серво
#define SERVO_BL_REAR_OFFSET  0   // Задняя левая нога, задний серво

#define SERVO_BR_FRONT_OFFSET 0   // Задняя правая нога, передний серво
#define SERVO_BR_REAR_OFFSET  0   // Задняя правая нога, задний серво
```

Убедитесь, что все оффсеты = 0.

---

### Шаг 3: Загрузка тестовой прошивки

**Вариант A: Использовать текущую прошивку**

Загрузите код на робота:
```bash
cd firmware/main_controller
platformio run --target upload
```

**Вариант B: Создать тестовый скрипт (рекомендуется)**

Создайте файл `firmware/main_controller/src/test_calibration.cpp`:

```cpp
#include <Arduino.h>
#include <Wire.h>
#include "SF_Servo.h"
#include "quadrupedal_data.h"

SF_Servo servos = SF_Servo(Wire);

void setup() {
    Serial.begin(921600);
    delay(1000);
    
    Serial.println("=== КАЛИБРОВКА СЕРВОПРИВОДОВ ===");
    
    // Инициализация I2C
    Wire.begin(1, 2, 400000);  // SDA=1, SCL=2, 400kHz
    
    // Инициализация PCA9685
    servos.init();
    servos.setAngleRange(0, 300);       // Диапазон 0-300°
    servos.setPluseRange(500, 2500);    // PWM 500-2500μs
    
    Serial.println("Серво инициализированы");
    Serial.println("Установка всех в позицию 90° (нейтраль)");
    
    // Установка всех серво в нейтральное положение 90°
    servos.setAngle(SERVO_FL_FRONT, 90 + SERVO_FL_FRONT_OFFSET);
    servos.setAngle(SERVO_FL_REAR,  90 + SERVO_FL_REAR_OFFSET);
    servos.setAngle(SERVO_FR_FRONT, 90 + SERVO_FR_FRONT_OFFSET);
    servos.setAngle(SERVO_FR_REAR,  90 + SERVO_FR_REAR_OFFSET);
    servos.setAngle(SERVO_BL_FRONT, 90 + SERVO_BL_FRONT_OFFSET);
    servos.setAngle(SERVO_BL_REAR,  90 + SERVO_BL_REAR_OFFSET);
    servos.setAngle(SERVO_BR_FRONT, 90 + SERVO_BR_FRONT_OFFSET);
    servos.setAngle(SERVO_BR_REAR,  90 + SERVO_BR_REAR_OFFSET);
    
    Serial.println("Готово! Проверьте положение ног.");
    Serial.println("Каждая нога должна быть вертикальной (90° к корпусу)");
}

void loop() {
    // Мигание для индикации работы
    static unsigned long lastBlink = 0;
    if (millis() - lastBlink > 1000) {
        Serial.println("Калибровка активна...");
        lastBlink = millis();
    }
    delay(100);
}
```

---

### Шаг 4: Определение правильной позы ноги

**Идеальная нейтральная поза (90°):**

```
Вид сбоку каждой ноги:

    Корпус
    ══════
       │
       │ ← Плечевой сустав (точка крепления)
       │
       ●───── Передний серво (90° = горизонтально вперед)
       │
       │
       ●───── Задний серво (90° = вертикально вниз)
       │
       │
       ○ ← Колесо на земле
```

**Критерии правильной позы:**
1. **Передний серво (FRONT):** Звено должно быть примерно горизонтально
2. **Задний серво (REAR):** Нога должна быть вертикальной (перпендикулярно корпусу)
3. **Все 4 ноги симметричны** относительно друг друга
4. **Высота одинаковая** у всех ног

---

### Шаг 5: Измерение оффсетов

Для каждого сервопривода, который **НЕ** в правильной позиции:

**Метод 1: Визуальная оценка**

1. Посмотрите на ногу
2. Оцените, на сколько градусов нужно повернуть серво
3. Запишите оффсет:
   - Если нужно повернуть **по часовой** → положительный оффсет (+10, +15, +20...)
   - Если нужно повернуть **против часовой** → отрицательный оффсет (-10, -15, -20...)

**Пример:**
```
Передняя левая нога (FL):
- Передний серво смотрит слишком вверх
- Нужно опустить на ~15°
- SERVO_FL_FRONT_OFFSET = -15

- Задний серво наклонен вперед
- Нужно повернуть назад на ~10°
- SERVO_FL_REAR_OFFSET = +10
```

**Метод 2: Снятие и повторная установка серворычага**

Если оффсет получается больше ±30°:

1. **Выключите питание**
2. Открутите винт серворычага
3. Снимите серворычаг
4. Установите его в позицию на 1-2 зуба ближе к правильной
5. Закрутите винт
6. Включите питание и проверьте
7. Теперь оффсет должен быть меньше

---

### Шаг 6: Внесение оффсетов в код

Откройте `quadrupedal_data.h` и внесите измеренные значения:

```cpp
// === ОФФСЕТЫ СЕРВОПРИВОДОВ (калибровка) ===
// ПЕРЕДНЯЯ ЛЕВАЯ НОГА (FL)
#define SERVO_FL_FRONT_OFFSET -15   // Было 0, теперь -15
#define SERVO_FL_REAR_OFFSET  +10   // Было 0, теперь +10

// ПЕРЕДНЯЯ ПРАВАЯ НОГА (FR)
#define SERVO_FR_FRONT_OFFSET +5    // Пример
#define SERVO_FR_REAR_OFFSET  -8

// ЗАДНЯЯ ЛЕВАЯ НОГА (BL)
#define SERVO_BL_FRONT_OFFSET 0     // Если нога уже правильная
#define SERVO_BL_REAR_OFFSET  +12

// ЗАДНЯЯ ПРАВАЯ НОГА (BR)
#define SERVO_BR_FRONT_OFFSET -20
#define SERVO_BR_REAR_OFFSET  +15
```

---

### Шаг 7: Проверка и итерация

1. **Перекомпилируйте и загрузите:**
   ```bash
   platformio run --target upload
   ```

2. **Проверьте позу:**
   - Включите робота
   - Все ноги должны быть симметричными и вертикальными

3. **Если нужна коррекция:**
   - Измените оффсеты на ±5°
   - Перезагрузите
   - Повторяйте до идеального результата

**Типичные значения оффсетов:** от -30° до +30°

---

## 🔍 Диагностика проблем

### Проблема: Нога выгибается в обратную сторону

**Причина:** Серворычаг установлен на 180° от правильной позиции

**Решение:**
1. Выключите питание
2. Снимите серворычаг
3. Поверните его на ~180° (обычно 9-10 зубцов)
4. Установите обратно

### Проблема: Серво дрожит/гудит

**Причины:**
- Недостаточное питание (проверьте 6V BEC)
- Механическое заедание (проверьте свободное вращение)
- Угол за пределами диапазона

**Решение:**
```cpp
// Проверьте диапазон в коде
servos.setAngleRange(0, 300);       // Должно быть достаточно
servos.setPluseRange(500, 2500);    // Стандартные значения
```

### Проблема: Одна нога сильно отличается от других

**Решение:**
1. Проверьте проводку (правильный канал PCA9685?)
2. Проверьте маппинг в `quadrupedal_data.h`:
   ```cpp
   #define SERVO_FL_FRONT 3   // Канал должен соответствовать физическому подключению
   ```

---

## 📏 Таблица каналов PCA9685

| Канал | Нога | Позиция | Макрос |
|-------|------|---------|--------|
| 1 | Передняя правая | Задний серво | SERVO_FR_REAR |
| 2 | Передняя правая | Передний серво | SERVO_FR_FRONT |
| 3 | Передняя левая | Передний серво | SERVO_FL_FRONT |
| 4 | Передняя левая | Задний серво | SERVO_FL_REAR |
| 5 | Задняя правая | Задний серво | SERVO_BR_REAR |
| 6 | Задняя правая | Передний серво | SERVO_BR_FRONT |
| 7 | Задняя левая | Передний серво | SERVO_BL_FRONT |
| 8 | Задняя левая | Задний серво | SERVO_BL_REAR |

**Проверка:** Отключите все серво кроме одного и проверьте, что канал соответствует ноге.

---

## 🎯 Быстрый чеклист калибровки

- [ ] 1. Все оффсеты установлены в 0
- [ ] 2. Загружена прошивка
- [ ] 3. Моторы отключены (безопасность)
- [ ] 4. Робот включен, ноги свободны
- [ ] 5. Проверена поза каждой ноги
- [ ] 6. Измерены необходимые оффсеты
- [ ] 7. Оффсеты внесены в quadrupedal_data.h
- [ ] 8. Перекомпилировано и загружено
- [ ] 9. Проверка: все ноги симметричны
- [ ] 10. Повторная коррекция если нужно (±5°)

---

## 💾 Пример откалиброванного файла

```cpp
// firmware/main_controller/include/quadrupedal_data.h

// === ОФФСЕТЫ СЕРВОПРИВОДОВ (ПОСЛЕ КАЛИБРОВКИ) ===

// Передняя левая нога (Front Left)
#define SERVO_FL_FRONT_OFFSET -12
#define SERVO_FL_REAR_OFFSET  +8

// Передняя правая нога (Front Right)  
#define SERVO_FR_FRONT_OFFSET +15
#define SERVO_FR_REAR_OFFSET  -5

// Задняя левая нога (Back Left)
#define SERVO_BL_FRONT_OFFSET -10
#define SERVO_BL_REAR_OFFSET  +18

// Задняя правая нога (Back Right)
#define SERVO_BR_FRONT_OFFSET +20
#define SERVO_BR_REAR_OFFSET  -15
```

**Эти значения индивидуальны для каждого робота!** Не копируйте - откалибруйте свой.

---

## 🔬 Продвинутая калибровка (опционально)

### Метод с Serial командами

Создайте интерактивную калибровку через Serial Monitor:

```cpp
void loop() {
    if (Serial.available()) {
        String cmd = Serial.readStringUntil('\n');
        cmd.trim();
        
        // Формат: "FL_FRONT +5" или "BR_REAR -10"
        // Парсинг команды и применение оффсета
        
        if (cmd.startsWith("FL_FRONT")) {
            int offset = cmd.substring(9).toInt();
            servos.setAngle(SERVO_FL_FRONT, 90 + offset);
            Serial.printf("FL_FRONT offset: %d\n", offset);
        }
        // ... аналогично для других серво
    }
}
```

Теперь можно экспериментировать с оффсетами в реальном времени через Serial Monitor!

---

## ⚠️ Важные замечания

1. **Калибровку делают ОДИН РАЗ** после сборки
2. **Оффсеты сохраняются в коде**, не в сервоприводах
3. **При замене серво** нужно перекалибровать эту ногу
4. **Механические повреждения** могут сбить калибровку
5. **Оффсеты от -30° до +30°** - нормально. Больше → переустановить серворычаг

---

**После калибровки робот должен стоять ровно, все ноги симметричные!** 🎉
